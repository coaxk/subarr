# Fail installs whose engines do not fit, instead of warning. Without this, a
# dev dependency needing a newer Node installed fine locally and then could
# not run in CI (jsdom 30 on CI Node 20). See package.json "//engines".
engine-strict=true

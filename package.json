{
  "name": "subarr-frontend-build",
  "version": "0.0.0",
  "private": true,
  "//engines": "Matches the node-version in .github/workflows. Declared because jsdom 30 silently installed here and could not load on CI Node 20: engines is ADVISORY by default, so npm install succeeded and the harness passed locally while never running in CI.",
  "engines": {
    "node": ">=20.19"
  },
  "description": "Build-time tooling for the subarr static SPA bundles. Not published.",
  "type": "module",
  "scripts": {
    "prepare": "node scripts/setup-hooks.mjs",
    "build:frontend": "node scripts/build-frontend.mjs",
    "dev:frontend": "node scripts/build-frontend.mjs --watch",
    "check:frontend": "node scripts/build-frontend.mjs && git diff --exit-code -- 'src/subarr/static/v1/home-hifi/*.bundle.js' 'src/subarr/static/v1/home-hifi/*.html'",
    "test:frontend": "vitest run"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "jsdom": "27.4.0",
    "vitest": "4.1.11"
  }
}
